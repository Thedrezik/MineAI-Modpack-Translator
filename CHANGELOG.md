# Журнал изменений MineAI Modpack Translator

Этот файл описывает наши локальные изменения поверх исходной beta-ветки
MineAI Modpack Translator. Исходной точкой была версия `10.0.0 - BETAv16`.
Журнал начинается с BETAv17 — первой сохранённой нами сборки.

Для каждой версии указаны назначение, изменённое поведение, основные файлы и
способ проверки. Список файлов показывает главные точки реализации, а не каждый
механически изменённый импорт или перевод интерфейса.

## Краткая история

| Версия | Дата | Основное изменение |
| --- | --- | --- |
| BETAv17 | 10.08.2026 | Защита Markdown и GuideME от повреждения переводом |
| BETAv18 | 10.08.2026 | Выбор модов, книг и квестов после анализа |
| BETAv19 | 10.08.2026 | Отдельное окно выбора и деление больших FTB-квестов |
| BETAv20 | 10.08.2026 | Удаление пустых архивов, фактический отчёт, reward tables |
| BETAv21 | 11.08.2026 | Поддержка LM Studio как отдельного локального провайдера |
| BETAv22 | 11.08.2026 | Исправления валидатора и повторного перевода локальным ИИ |
| BETAv23 | 11.08.2026 | Защита `#variables#`, ремонт маркеров и контроль языка |
| BETAv24 | 11.08.2026 | Поиск корневых книг и исправления Markdown по журналам LM Studio |
| BETAv25 | 11.08.2026 | Независимое сохранение перевода и кодов форматирования Patchouli |
| BETAv26 | 11.08.2026 | Контроль порядка разметки, качества пакетного ИИ и активация ресурспака |
| BETAv27 | 11.08.2026 | Lossless-перевод книг без передачи разметки модели |
| BETAv28 | 11.08.2026 | Перевод смысловых Markdown-абзацев вместо оборванных физических строк |
| BETAv29 | 11.08.2026 | Общий AST документов, структурное обновление SNBT и сохранение выбора |
| BETAv30 | 12.08.2026 | FormatKit SDK, детерминированный skeleton книг и реальные корпусные проверки |
| BETAv31 | 12.08.2026 | Полнота встроенных книг, перевод tooltip и сегментный fallback |
| BETAv32 | 12.08.2026 | Общие книги между JAR, Modonomicon и руководства дополнений IE |
| BETAv33 | 13.08.2026 | Смысловые списки GuideME, корректная проверка якорей и сброс ошибочного кэша |
| BETAv34 | 13.08.2026 | Lossless-коды, честный статус и установка датапака через доступный загрузчик |
| BETAv35 | 13.08.2026 | Послойное восстановление кэшей через локальный ИИ и Google fallback |
| BETAv36 | 13.08.2026 | Lossless-разделители после цветного текста во всех форматах книг |
| BETAv37 | 13.08.2026 | Отдельный проект с объединённым MineAI-FormatKit и двойной валидацией |
| BETAv38 | 13.08.2026 | Датапак каждого мира и стабильные DescriptionId Modonomicon |
| BetaAll_40 | 13.08.2026 | Поддержка всех версий Minecraft из текущего топ-100 CurseForge |

---

## 10.0.0 — BetaAll_40

Дата сборки: 13.08.2026.

### Что изменили

- Добавлены версии Minecraft `1.7.10`, `1.21.11`, `26.1.2` и `26.2`.
- Для них заданы корректные форматы resource pack и data pack.
- Добавлены отдельный PyInstaller-профиль и имя EXE
  `MineAI_Translator_BetaAll_40.exe`.
- Добавлены регрессии, проверяющие доступность версий в интерфейсе и
  `pack_format` созданного архива для Minecraft 26.2.

### Проверка

- 560 тестов и 132 вложенные проверки проходят; 9 необязательных корпусных
  тестов пропускаются при отсутствии внешних бинарных фикстур.
- Реальный FTB Evolution: 41 FTB-файл и 7 682 единицы прошли identity и
  synthetic reconstruction без ошибок.

---

## 10.0.0 — BETAv38

Дата сборки: 13.08.2026.

### Задача

Исключить любые изменения `minecraft/kubejs/data`, устанавливать результат
данных отдельно в каждый существующий мир и перенести русские тексты
Modonomicon из data JSON в ресурс-пак.

### Что изменили

- Все пути `data/...` сначала собираются в проверенный мастер-архив
  `MineAI_Datapacks/<имя>_Datapack.zip`. OpenLoader и KubeJS больше не являются
  целями записи.
- После успешного закрытия и CRC-проверки архива его одинаковые байты атомарно
  копируются во все прямые каталоги `saves/<мир>/datapacks`, где присутствует
  `level.dat`. Служебные папки и каталоги без настоящего мира пропускаются.
- Установка транзакционная: при ошибке копирования уже созданные файлы
  удаляются, а ранее существовавшие версии восстанавливаются. Ошибка упаковки
  не оставляет частичный датапак ни в одном мире.
- `minecraft/kubejs/data` не читается как место установки и никогда не
  изменяется. Добавлена регрессия с контрольным файлом KubeJS.
- Литеральным полям Modonomicon назначаются детерминированные ключи вида
  `mineai.book.<namespace>.<book>.<relative_path>.<json_path>`. Ключ зависит от
  логического пути и позиции поля, поэтому повторный перевод или другой движок
  не меняют `DescriptionId`.
- В data JSON остаются стабильные ключи, рецепты, условия, ID и исходная
  структура. Английские значения записываются в `assets/<namespace>/lang/en_us.json`,
  переведённые — в целевой lang-файл ресурс-пака. Существующие locale JSON
  объединяются без потери ключей.
- Отчёт завершения отдельно показывает мастер-датапак и каждый мир, куда он
  установлен. Если сохранённых миров нет, программа сообщает о ручной
  установке мастер-архива или необходимости повторного запуска после создания
  мира.
- Любой абсолютный путь, NUL или сегмент `..` отклоняется до записи в архив.

### Проверка

- Интеграционный тест создаёт два настоящих временных мира и одну постороннюю
  папку: одинаковый датапак появляется только в двух мирах, а контрольный файл
  `kubejs/data` остаётся неизменным.
- Интеграция Pagan Guide подтверждает замену `name`, `description` и текста
  страницы на стабильные ключи, а также наличие исходных и русских значений в
  соответствующих lang-файлах ресурс-пака.
- Проверяется lenient-многострочный JSON Modonomicon и запрет path traversal.

---

## 10.0.0 — BETAv37

Дата сборки: 13.08.2026.

### Задача

Создать отдельный итоговый проект на базе Beta36 и приватного SDK
`LifeViwer/MineAI-FormatKit`, не заменяя проверенные смысловые блоки менее
контекстным построчным разбором и не теряя существующие адаптеры.

### Что изменили

- В проект встроен самостоятельный пакет `mineai_formatkit` с 15
  корпусно-проверенными адаптерами. UI, сканирование, кэш, движки и упаковка
  остаются ответственностью приложения.
- Добавлен bridge между новым `TranslationPlan` и контрактом Beta36. Для
  GuideME и IE manual сохраняются контекстные блоки Beta36, после сборки
  bridge сначала проверяет, что SDK покрывает все видимые span старого плана.
  Совместимые документы проходят вторую SDK-реконструкцию; несовместимые
  варианты (например переводимый alt-текст изображения) безопасно остаются на
  lossless-плане Beta36 вместо ложного отказа после перевода.
- Обычные Minecraft locale JSON теперь переводятся через точные span-единицы
  SDK. Исходные отступы, порядок ключей и экранирование не переформатируются.
- Patchouli JSON переводится новым адаптером: `$(...)`, `/$`, рецепты, ID и
  технические поля не попадают в запрос ни локального ИИ, ни Google.
- Для реальных устаревших вариантов добавлен контролируемый fallback: locale
  JSON с конфликтующими дубликатами ключей и Patchouli со строковым массивом
  `pages` обрабатываются проверенным парсером Beta36. Анализ, оценка строк и
  процессор выбирают fallback одинаково.
- Добавлена фактическая обработка Oracle Index `.mdx` и `_meta.json` с выводом
  в `.translated/<locale>`. Анализ, оценка количества и процессор используют
  один план.
- Modonomicon, Heracles, XML, properties и прочие уникальные адаптеры Beta36
  оставлены резервным покрытием.
- Исходный SDK и его документация сохранены отдельно; тесты приложения и SDK
  запускаются независимыми наборами.

### Проверка

- 416 тестов приложения и 39 вложенных проверок проходят.
- 139 тестов MineAI-FormatKit и 93 вложенные проверки проходят, 9 необязательных корпусных тестов
  пропущены при отсутствии внешних бинарных фикстур.
- Интеграционные регрессии подтверждают lossless locale JSON, Patchouli без
  разметки в запросе, Oracle Index и двойную проверку GuideME/IE.
- Финальный read-only аудит 9 установленных Prism-сборок проверил 2 929 JAR,
  2 250 файлов книг/руководств и 8 692 locale JSON. Получено 24 071 единиц книг
  и 25 197 locale-единиц, найдено 1 226 перемещаемых зависимостей; ошибок
  identity/synthetic apply — 0.

### Ограничение публикации

В приватном репозитории `LifeViwer/MineAI-FormatKit` на момент объединения нет
файла лицензии. Публиковать объединённые исходники или бинарник следует только
после добавления лицензии либо явного разрешения владельца SDK.

---

## 10.0.0 — BETAv36

Дата сборки: 13.08.2026.

### Задача

Исключить слипание обычного текста с цветным фрагментом, когда переводчик
удаляет пробел после reset-кода Minecraft: `§r. However` превращался в
`§r.Однако`. Структура пробелов и пунктуации книги должна восстанавливаться из
английского источника, а не зависеть от ответа модели.

### Что изменили

- Общий tokenizer теперь считает reset-код вместе с непосредственно следующим
  разделителем единым неизменяемым элементом skeleton: `§r. `, `§r, `,
  `§r ` и соответствующие варианты `&r`.
- Пунктуация и горизонтальный пробел после цветного текста больше не передаются
  локальному ИИ, Google, DeepL или OpenRouter и возвращаются из оригинала
  побайтно при сборке результата.
- Правило подключено как к FormatKit-адаптерам Markdown, IE manual, обычных
  текстовых книг, XML и properties, так и к общему rich-text конвейеру
  JSON/Patchouli/Modonomicon.
- Изменившийся детерминированный payload получает новый ключ кэша. Поэтому
  сохранённый ранее дефектный вариант не используется повторно и строка
  переводится заново без ручной очистки всего кэша.
- Добавлены регрессии для реального класса ошибки IE `§2word§r. Next` в
  IE manual, Markdown и JSON-книгах.

### Фактическая проверка

- Реальный `assets/immersiveengineering/manual/en_us/railgun.txt` из
  FTB Evolution проверен через FormatKit: исходная строка с graphite electrodes
  восстанавливается без изменения структуры.
- Все 402 автоматических теста проходят.

---

## 10.0.0 — BETAv35

Дата сборки: 13.08.2026.

### Задача

Добавить отдельный безопасный режим восстановления переводов из кэшей любых
версий программы. Обычный режим обработки при этом не должен скрывать старые
или неполные строки в уже существующих локализациях.

### Что изменили

- В карточке режима добавлен флажок `Восстановить из AI-кэша`. Он блокирует
  `Дополнить / Пропустить / Заново`, принудительно использует английский
  источник и автоматически выбирает локальный ИИ: KoboldCPP или LM Studio.
- Для каждой строки выполняется утверждённая цепочка:
  `AI-кэш → Google-кэш → локальный ИИ → Google fallback`.
- Каждый найденный результат снова проходит текущую проверку языка, переносов,
  защищённых переменных, игровых кодов и FormatKit-структуры. Повреждённая
  запись удаляется только из своего слоя; остальные записи кэша сохраняются.
- Валидная строка из Google-кэша повышается в AI-кэш. Исправления обоих кэшей
  сохраняются атомарно, даже если перевод завершился без обращения к модели.
- Кэши прежних версий больше не сбрасываются целиком. Создаётся единая копия
  `.pre-auto-repair`, версия проверки обновляется, а записи мигрируют и
  проверяются по одной. Поддерживается UTF-8 с BOM и числовое поле версии.
- Усилена проверка явного усечения: длинная фраза из нескольких слов больше не
  принимается как случайное одно короткое слово.
- При отказе локального ИИ Google fallback обязателен именно в режиме
  восстановления, независимо от обычной настройки доперевода.
- Настройка флажка сохраняется вместе с языком, версией Minecraft и движком.

### Фактическая проверка

- Все 399 автоматических тестов проходят.
- Отдельно проверены: повреждённый AI-кэш, валидный Google-кэш, повышение
  результата между слоями, отказ локального ИИ, обязательный Google fallback,
  старые версии кэша, принудительный режим процессоров и состояние Qt-интерфейса.

---

## 10.0.0 — BETAv34

Дата сборки: 13.08.2026.

### Задача

Устранить регрессии реального прогона BETAv33 без правил для отдельных модов:
массовые ложные отказы цветных строк Modonomicon, потерю конечных переносов,
ошибку `The UI`, обработку ключей локализации со слешами и ложный успешный
статус при фактически непереведённых строках. Исключить ложную установку
датапака в `config/openloader/data`, когда OpenLoader отсутствует в сборке.

### Что изменили

- Общий tokenizer игровых кодов атомарно защищает полное семейство
  Modonomicon `[#](RRGGBB)` / `[#]()` с исходным регистром и пробелами. Эти
  конструкции больше не разбираются как Markdown-ссылки, а модель никогда не
  получает цвет или reset в переводимом payload.
- MarkdownAdapter и общий rich-text skeleton используют тот же токен, поэтому
  защита одинакова для локального ИИ, OpenRouter, Google и DeepL.
- Постобработка детерминированно возвращает исходные начальные и конечные
  пробелы, LF и CRLF после очистки ответа. Модель отвечает только за текст и не
  должна воспроизводить физическую границу строки.
- Допускается только безопасное удаление английского артикля перед полностью
  технической подписью (`The UI` → `UI`). Обычный английский текст по-прежнему
  не может пройти проверку без перевода.
- Адаптер Modonomicon распознаёт ключи локализации с ресурсным путём, например
  `book.mod.guide.mod/entry.page0.text`. Идентификатор не отправляется модели,
  а реальный английский текст берётся из companion `en_us.json`.
- AI-кэш версии 27 обновляется до версии 28 выборочно: создаётся резервная копия
  `ai_cache.json.pre-beta34`, валидные записи сохраняются, а повреждённые
  удаляются общей проверкой при загрузке или первом использовании.
- Финальный статус учитывает `failed_strings`. При частично сохранённом
  оригинальном тексте приложение пишет «Завершено с ошибками» и точное число,
  а не сообщает о полном успехе.
- Ресурспак и датапак по-прежнему создаются только при наличии реальных файлов.
  Для буквальных страниц Modonomicon датапак необходим, а локализации
  `assets/.../lang` остаются в ресурспаке.
- Способ установки data-части определяется по настоящим `modId` внутри JAR,
  поэтому переименованные файлы модов также распознаются. При наличии
  OpenLoader используется его `config/openloader/data`; без OpenLoader, но с
  KubeJS, файлы атомарно устанавливаются в активный `kubejs/data` и выводятся
  в списке фактически изменённых путей.
- Если в сборке нет ни OpenLoader, ни KubeJS, программа сохраняет исправный
  архив в `MineAI_Datapacks` и прямо сообщает, что его нужно установить в
  `datapacks` выбранного мира. Неиспользуемая папка OpenLoader больше не
  создаётся, а ошибка финальной проверки откатывает запись в KubeJS. Перед
  loose-записью проверяется реальный путь: выход из `kubejs/data` через `..`
  или ссылку блокируется.

### Фактическая проверка

- 394 автоматических теста проходят, включая отдельные сценарии LM Studio и
  Google для цветов, CRLF, технических подписей, кэша и терминального статуса.
- Реальный архив BETAv33 с 13 документами
  `paganbless:pagan_guide` побайтово перенесён тестовым PackWriter в
  `kubejs/data`: 13/13 совпадений. В исходном результате уже подтверждены
  переводы `Herbalist Bench` и `Watch your fingers!`; причиной английского
  текста был отсутствующий в FTB Evolution 1.42.0 OpenLoader.
- Проверены все 9 доступных каталогов сборок с папкой `mods`: две сборки
  используют OpenLoader, семь — KubeJS; для каждой выбран существующий в ней
  механизм, определённый по метаданным модов.
- Read-only FormatKit-аудит 10 локальных сборок: 2929 JAR, 2250 файлов книг,
  24 071 смысловой блок, 25 185 JSON-строк и 0 структурных ошибок.
- Дополнительный аудит Modonomicon: 3192 просмотренных JAR, 1436 документов,
  7828 цветовых токенов в английских lang-файлах и 0 ошибок маскирования или
  обратной сборки.

---

## 10.0.0 — BETAv33

Дата сборки: 13.08.2026.

### Задача

Устранить регрессию BETAv32, из-за которой корректный русский перевод мог
отклоняться около защищённых тегов, а переносы внутри списков GuideME
отправлялись переводчику как несвязанные строки и сдвигали смысл соседних
пунктов.

### Что изменили

- FormatKit проверяет полноту всего видимого смыслового блока, поэтому артикль
  `The` может корректно исчезнуть, а русский порядок слов — измениться около
  `ItemLink`, игрового тега или физического переноса строки.
- Подписи Markdown- и IE-ссылок по-прежнему проверяются отдельно: оставленная
  английская подпись отклоняется, тогда как адрес, ID и оболочка ссылки
  остаются неизменными.
- Продолжение пункта Markdown-списка объединяется с первой строкой в один
  `TranslationUnit`. Реальная конструкция AdvancedAE с переносом после
  `ItemLink` больше не разрывает фразы `No empty spaces ... for no additional
  benefits` на два независимых запроса.
- Форматированные блоки с защищёнными якорями отправляются пакетами не более
  пяти элементов. Ограничение действует одинаково для LM Studio, KoboldCPP,
  OpenRouter и Google и уменьшает вероятность смыслового сдвига соседних
  значений при пакетном переводе.
- Версии адаптеров повышены до `guideme-v2` и `markdown-v2`.
- Версия AI-кэша повышена до `27`. При первом запуске BETAv33 кэш BETAv32
  сохраняется как `ai_cache.json.pre-beta33`, после чего создаётся чистый кэш;
  ошибочные соответствия из прерванного запуска не используются повторно.
- `ai_error_log.txt` теперь явно называет записи не окончательными ошибками, а
  неудачными промежуточными попытками, которые могут быть восстановлены
  повтором или Google fallback.

### Фактическая проверка

- 381 автоматический тест, включая прямые сценарии локального ИИ и Google.
- На реальной странице
  `advanced_ae/ae2guide/aae_intro/quantum_computer.md` из FTB Evolution 1.42.0
  продолжение первого пункта собрано в один блок; no-op FormatKit побайтово
  совпадает с оригиналом.
- Проверены сохранность `ItemLink`, переводимые подписи Markdown/IE-ссылок,
  строгий повтор Google и общий fallback после структурного отклонения.

---

## 10.0.0 — BETAv32

Дата сборки: 12.08.2026.

### Задача

Полностью устранить обнаруженные в FTB Evolution дефекты книг
`modern_industrialization:guidebook`, `paganbless:pagan_guide` и
`immersiveengineering:manual`, не добавляя правил для отдельных названий модов.

### Что изменили

- Карта локализованных каталогов Markdown теперь строится один раз по всей
  сборке. Дополнение из отдельного JAR наследует формат локали основной книги.
  Поэтому страницы Industrialization Overdrive попадают в
  `mi_guidebook/_ru_ru`, рядом с основной книгой Modern Industrialization, а
  относительная ссылка `../steam_age/coke_oven.md` больше не ведёт в
  отсутствующую ветку `ru_ru`.
- То же общее правило применяется к любым книгам, расширяемым несколькими JAR.
  На установленных сборках такие случаи дополнительно найдены у AE2 и RFTools.
- Добавлен lossless-адаптер `modonomicon-json-v1`. Он извлекает только
  отображаемые `name`, `title`, `description`, `text`, tooltip и аналогичные
  поля, защищая типы страниц, ID, предметы, рецепты, условия, координаты и
  зависимости.
- Адаптер понимает фактический Gson-lenient диалект Modonomicon, включая
  многострочные строки Pagan Blessing, которые не принимает строгий
  `json.loads`. Неизменённый документ проходит побайтовый no-op, а после
  перевода сверяется полный нетекстовый skeleton.
- Литеральные страницы Modonomicon записываются в датапак по исходному
  `data/<namespace>/modonomicon/books/...` пути. Значения, заданные ключами
  локализации, переводятся отдельно в
  `assets/<namespace>/lang/ru_ru.json` ресурс-пака.
- IE manual-адаптер теперь распознаёт руководства дополнений в любом
  `assets/<namespace>/manual/<locale>`, включая Engineered Schematics и
  Immersive Petroleum. Сопутствующие `manual.<namespace>.*` ключи также
  включаются при выбранном режиме «Книги и тексты».
- Проверка FormatKit контролирует полноту каждого видимого сегмента между
  защищёнными ссылками. Частично русский ответ с оставленной английской
  подписью `<link;...;multiblock machines>` отклоняется и повторно переводится,
  при этом назначение ссылки остаётся неизменным.
- Анализатор, оценщик и фактический процессор используют одну общую карту книг,
  один Modonomicon-план и одинаковый набор сопутствующих lang-ключей.
- Ошибка разбора одного повреждённого Modonomicon-документа изолируется и не
  мешает собрать lang-ключи из остальных корректных документов того же JAR.

### Фактическая проверка

- Полный набор: 375 автоматических тестов.
- Просканировано 9 установленных каталогов `minecraft`, 2922 JAR.
- Проверено 1360 реальных Modonomicon JSON: 634 литеральных переводимых узла,
  7011 ссылок на lang-ключи, 0 ошибок no-op и структурного применения.
- Для 7006 из 7011 ссылок найден исходный английский текст. Оставшиеся пять
  ссылок — три уникальных отсутствующих ключа в исходных JAR Occultism/Nautec;
  это зафиксированные дефекты содержимого модов, а не потеря при сканировании.
- Проверено 1214 реальных файлов IE manual: 11084 смысловых блока, 0 ошибок
  no-op и структурного применения.
- Для Modern Industrialization + Industrialization Overdrive проверены 42
  страницы и все 5 относительных Markdown-ссылок: отсутствующих целей нет.
- На реальных JAR FTB Evolution анализ и запуск совпали: Engineered Schematics
  33/33, Industrialization Overdrive 16/16, Modern Industrialization 326/326,
  Pagan Blessing 82/82.
- Пробная упаковка Pagan Blessing создала 13 изменённых документов в датапаке
  и отдельный книжный lang-словарь в ресурс-паке; CRC и повторный строгий разбор
  JSON завершились успешно.

---

## 10.0.0 — BETAv31

Дата сборки: 12.08.2026.

### Задача

Завершить перевод книг после реального прогона BETAv30 на FTB Evolution:
исправить тихо сохранённые английские страницы, текст внутри Patchouli-tooltip
и последние блоки, которые LM Studio и Google не могли вернуть с полным
набором структурных маркеров.

### Что изменили

- Режим «Дополнить» больше не доверяет любому существующему значению в
  `ru_ru`. Встроенная страница считается готовой только после проверки языка и
  полноты; английские и частично переведённые страницы заново строятся из
  актуального `en_us` skeleton.
- Текст Patchouli-подсказок `$(t:...)` отделён от неизменяемой оболочки. Модель
  получает содержимое tooltip как обычный текст, а префикс, закрывающую скобку,
  ссылки, конфигурационные ID и остальные игровые коды возвращает программа.
- Добавлен детерминированный последний fallback для FormatKit-блоков. Если
  движок после повторов повреждает якоря или не возвращает большой блок,
  MineAI переводит только видимые сегменты между якорями. Длинные сегменты и
  узлы без якорей дополнительно делятся по границам предложений, после чего
  результат собирается по исходному skeleton. Ссылки, теги, переносы и порядок
  элементов при этом не зависят от ответа модели.
- Устранён ложный отказ уже готового перевода: прежняя проверка удаляла якоря
  и склеивала соседние текстовые фрагменты в мнимый синтаксис `.Apotheosis`,
  `.RegexOne`, `r(` или `.x`. Теперь каждый видимый сегмент проверяется отдельно,
  а форматированный JSON проходит тот же FormatKit-validator до записи в кэш.
- Контроль неполного ответа теперь обнаруживает английскую инструкцию длиной
  от четырёх слов внутри русского текста. Короткие названия продуктов без
  английской служебной лексики остаются допустимыми.
- Латинские биномиальные названия в ссылках Productive Bees, например
  `Xylocopa aerata` и `Halictus rubicundus`, классифицируются как научные
  идентификаторы. Они сохраняются намеренно и больше не учитываются как ошибки
  перевода.
- Анализатор, оценщик и процессоры JAR/loose JSON используют одну проверку
  готовности существующих книжных узлов, поэтому число ожидаемых строк и
  фактическая обработка остаются согласованными.
- Повреждённый встроенный `ru_ru.json` больше не скрывает исправный английский
  словарь целиком. Анализатор считает исходные ключи, считает локаль пустой и
  согласуется с режимом «Дополнить»; это исправило реальные Immersive Petroleum
  и Occultism.
- Старые datapack-книги Patchouli из
  `data/<namespace>/patchouli_books/<book>/en_us` переводятся в такой же
  `ru_ru`-путь и попадают в датапак. Правило основано на структуре Patchouli,
  а не на списке известных модов.
- Явно локализованные `.txt` распознаются по каталогу `en_us` даже вне папок с
  именами `guide`/`manual`. Так в общий lossless-конвейер вошли мини-игры
  Alex's Caves и аналогичные будущие ресурсы.
- Добавлена полноценная поддержка старых Forge-словарей `en_US.lang` и
  `en_us.lang`: значения переводятся через `properties-v1`, ключи, комментарии,
  разделители и переносы остаются неизменными. Анализ, подсчёт, ресурспак и
  безопасная запись прямо в JAR используют один target-path.
- Сканируются исходные ZIP-ресурспаки и распакованные ресурспаки. ZIP включается
  только при наличии английского переводимого источника, поэтому созданные
  MineAI архивы с одним `ru_ru` не попадают в рекурсивную обработку.
- Встроенные пути вида `packs/.../assets/.../lang`, `data/<mod>/lang` и
  `<mod>/lang` нормализуются в реальный путь ресурспака
  `assets/<mod>/lang`. Несколько словарей с одним итоговым путём объединяются
  по ключам перед упаковкой вместо тихой потери второго файла или создания
  дубликатов ZIP-entry.
- Служебные JSON сторонних приложений, например
  `crash_assistant_localization`, больше не выдаются за Minecraft-lang и не
  расходуют запросы перевода: ресурспак не способен переопределить такой путь.
- Внешний аудит публичных сборок добавил общий поиск распакованных ресурсов в
  `config`, `kubejs`, `packmenu`, `resourcepacks`, `defaultconfigs` и
  `.minecraft/patchouli_books`. MineAI определяет назначение по структурному
  пути: вложенные `assets` отправляются в ресурспак, вложенные `data` — в
  датапак, а форматы, которые игра читает непосредственно из `config` или
  `patchouli_books`, получают соседний каталог `ru_ru` на диске.
- Поддержаны внешние Patchouli-книги сборки, старые книги из
  `kubejs/data/<namespace>/patchouli_books`, языковые ресурсы PackMenu и
  вложенные mod-resource roots вида `config/<mod>/resources/assets`.
- Markdown-документация Project Intelligence теперь проходит через тот же
  lossless FormatKit, что и книги внутри JAR. Отдельный JSON-индекс
  `structure/lang/en_us.json` переводит только поле `translation`; `page_uri`,
  `page_rev`, порядок массива и остальные метаданные сохраняются.
- Внешние словари BetterQuesting `config/.../lang/en_us.lang` обрабатываются
  адаптером `properties-v1` и возвращаются как соседний `ru_ru.lang`, поскольку
  такой путь не является содержимым обычного ресурспака.
- Добавлен отдельный `heracles-quest-v1` для Heracles/Odyssey Quests. Он
  обнаруживает `config/heracles/quests/**/*.json`, выделяет только видимые
  компоненты, Markdown/Hermes-описания, безопасные подписи задач/наград и
  названия групп. ID, `type`, команды, зависимости, предметы, NBT, предикаты,
  координаты и награды не отправляются переводчику и входят в структурный
  fingerprint.
- `groups.txt` и `tutorial.html` Heracles получили отдельные lossless-адаптеры.
  Повторяющиеся названия групп используют общий cache-scope, поэтому JSON и
  боковая панель получают один перевод. Поскольку Heracles читает описание
  только из `config`, результат записывается атомарно на место; английский
  оригинал хранится в проверенном `.bak`, а неподтверждённый старый baseline
  сохраняется как `.stale-<hash>`.
- Учтены обе реально встречающиеся формы Minecraft Component: обычная строка,
  `{"text":"..."}` и `{"translate":"Visible English text"}`. Настоящие
  ключи вида `pack.quest.title` защищены, а человекочитаемое значение поля
  `translate`, которое создаёт редактор Heracles, переводится.

### Проверка

- В реальном `Beta30_LASTFIX_LM_Yandex.zip` проверено 2019 файлов, 6655
  FormatKit-единиц, 5033 книжных JSON-узла и 1240 lang-значений.
- Найдены 8 финальных ошибок журнала, 28 английских Patchouli-tooltip и
  встроенные английские страницы Apotheosis, Just Dire Things, Modular Routers,
  PneumaticCraft и других книг; все причины покрыты общими правилами без
  специальных условий для конкретной сборки.
- Проведён независимый no-op round-trip всех распознанных книг в девяти
  фактически существующих сборках PrismLauncher: ошибок структуры — `0`.
- Для каждой сборки независимо сопоставлены анализ и предрасчёт запуска. Во
  всех девяти случаях расхождение равно `0`, включая ATM 10 (`96857`), FTB
  Evolution (`82929`), StoneBlock 4 (`54999`) и MC Eternal 2 (`56878`)
  ожидающих строк.
- Полный набор: 348 тестов успешно.
- Дополнительный GitHub-корпус охватил ATM 10, Enigmatica 6, Enigmatica 9,
  GreedyCraft и Supersymmetry: распознаны все `419` найденных английских
  locale/book-файлов (`0` пропусков по поддерживаемым расширениям), включая
  `208` Markdown-страниц Project Intelligence, `158` внешних книжных JSON,
  PackMenu, KubeJS data books и legacy `.lang`. Анализ всех пяти репозиториев
  завершился без исключений.
- Отдельный корпус Heracles включил Greate: Beyond the Horizon, Tinkerer's
  Quilt, Create: Down to Earth, SwagPack и тестовую сборку realliance:
  проверено `360` реальных файлов и `4162` переводимых единицы. No-op и
  синтетическое применение перевода прошли без единой ошибки структуры;
  полный набор после добавления адаптера — `367` тестов.

---

## 10.0.0 — BETAv30

Дата сборки: 12.08.2026.

### Задача

Устранить фактические повреждения и дубли GuideME/AE2, неполный перевод
Immersive Engineering и расхождение анализа со стартовым подсчётом. Вынести
работу с книжными форматами в самостоятельный SDK: приложение выбирает и читает
файл, FormatKit отвечает только за разбор, переводимые payload, обратную сборку
и доказательство сохранности структуры.

### Что изменили

- Добавлена независимая библиотека `formatkit` без GUI, файлового сканера,
  архиватора, кэша и сетевых движков. Публичный контракт состоит из
  `FormatRegistry`, `TranslationPlan`, `TranslationUnit`, `ApplyResult` и
  `ValidationReport`.
- `JarProcessor`, анализатор и оценщик книг используют один и тот же
  `TranslationPlan`. На тестовой `ftb-evolution-1.41.1` фактические значения
  анализа и запуска теперь равны: `11618 == 11618`.
- GuideME всегда записывается в корректный locale-каталог `_ru_ru`. Проверка
  готового Beta29-пака подтвердила, что прежняя версия реально создавала
  `ae2guide/ru_ru`; из-за этого GuideME одновременно показывал английское и
  локализованное дерево страниц и нарушал относительное разрешение ресурсов.
- Markdown/MDX разбирается в детерминированный skeleton. Целые абзацы остаются
  контекстными единицами; ссылки, адреса, теги, `ItemLink`, `ImportStructure`,
  сцены, таблицы, front matter, fenced code и физические переносы модель не
  может изменить.
- Ссылки и теги, перенесённые автором на несколько физических строк, теперь
  распознаются целиком. Это исправляет реальные страницы MAE2, которые прежний
  однострочный tokenizer считал повреждёнными.
- Добавлен специализированный адаптер Immersive Engineering. В
  `<link;target;label>` переводится только label, в том числе с апострофом;
  target и управляющая разметка восстанавливаются из английского оригинала.
- Ключи `manual.immersiveengineering.*` из `lang/en_us.json` включаются в
  перевод книги даже при выключенной области «Моды», поэтому названия разделов
  IE больше не остаются английскими.
- Generic-адаптер поддерживает `.md`, `.markdown` и `.txt`, включая фактически
  найденные ссылки `{label|target}` книг Alex's Caves/Citadel.
- Добавлен общий tokenizer Minecraft-кодов: `§`/`&`, Patchouli `$()`, printf,
  placeholders, экранирование и форматные маркеры защищаются одинаково для
  локального ИИ и Google, поскольку оба движка получают payload через общий
  `TranslationService`.
- Существующие переводы в режиме «Дополнить» накладываются на английский
  skeleton по структуре единиц. Уже переведённые фрагменты превращаются в
  неизменяемые якоря, а новый английский текст переводится в полном контексте.
- Кэш книжных payload изолирован по adapter ID, пути файла и unit ID. Ответ,
  прошедший общий валидатор, но нарушивший формат конкретной книги, удаляется из
  кэша.
- `apply_resilient()` сохраняет корректные единицы страницы, а только
  повреждённую единицу возвращает к английскому оригиналу. Одна ошибка модели
  больше не уничтожает перевод всего файла.
- `ValidationReport` содержит SHA-256 fingerprints исходного и итогового
  skeleton. Проверяются порядок/значение якорей, разметка, таблицы, переносы и
  неизменяемые области.
- Исправлена ошибочная классификация обычных слов с точкой (`dimensions.`,
  `energy.`) как технических идентификаторов. Файлы наподобие `guide.md`
  по-прежнему защищаются.
- JSON-книги теперь определяются по явному locale-сегменту `/en_us/`, а не по
  списку названий движков. Это добавило реально пропущенные книги Tinkers'
  Construct/Hephaestus в обеих сборках Create Astral и MC Eternal без правил
  для конкретных модов.
- Выборка JSON больше не ограничена семью точными именами полей. Общая
  семантика ключей покрывает `effects`, `properties`, `header`, `heading`,
  `title1`, `usage.text`, `spotlightText` и аналогичные варианты; поля ID и
  входов не затрагиваются. Если `text` содержит массив объектов, обход
  рекурсивно доходит до внутренних абзацев.
- Добавлены lossless-адаптеры `properties-v1` и `xml-text-v1`. Они покрывают
  найденные `language.lang` Tinkers' Construct и XML-страницы LDLib, сохраняя
  ключи, комментарии, теги, атрибуты, entities, отступы и переносы.
- При переносе документа в locale-каталог приложение вычисляет относительные
  нетекстовые зависимости и копирует только существующие `.snbt`, `.nbt`,
  изображения и другие ресурсы в соответствующий локализованный путь. Так
  `ImportStructure src="assets/assemblies/meteor_interior.snbt"` продолжает
  разрешаться из `_ru_ru/getting-started.md`.
- Loose-словари вида `config/<namespace>/lang/en_us.json` обнаруживаются
  рекурсивно и попадают в ресурспак как
  `assets/<namespace>/lang/<locale>.json`. На FTB Evolution так найдены два
  ранее невидимых словаря; пустые значения по-прежнему не переводятся.
- Loose-книги внутри `kubejs/assets/**/en_us/*.json` проходят ту же структурную
  выборку, что книги в JAR. В двух версиях Create Astral найдены шесть таких
  страниц с 20 переводимыми узлами; output сохраняет исходный namespace и
  меняет только locale-сегмент.
- Поиск JAR получил детерминированный порядок. Повторный запуск на одной и той
  же сборке больше не зависит от порядка, возвращённого файловой системой.
- Анализатор перестал считать слово `guide` внутри namespace признаком книги.
  Обычный `assets/guideme/lang/en_us.json` больше не исчезает из анализа.
- Метаданные книги IE не учитываются повторно, когда одновременно включены
  области «Моды» и «Книги».
- По журналу реального прогона Beta30 найдены 305 ответов, которые уже были
  переведены движком, но затем возвращались к английскому оригиналу: 303
  Markdown-блока и два блока IE. Причиной был добавленный моделью пробел рядом
  с защищённым переносом. FormatKit теперь восстанавливает пробелы на границах
  якорей из исходного skeleton; фактические четыре пропущенных абзаца страницы
  AE2 P2P применяются без изменения структуры.
- Проверка FormatKit выполняется до записи ответа в кэш. Реальное перемещение
  текста через границу ссылки или выделения вызывает строгий локальный повтор,
  а при включённой настройке — Google fallback. Повреждённый кандидат больше не
  принимается общим валидатором и не превращается в сохранённый английский блок.
- Форматированные JSON-строки Patchouli теперь отправляются как один смысловой
  блок с неизменяемыми якорями. Фрагменты вроде `The`, `to receive.` и `/$ or`
  больше не переводятся отдельно от предложения.
- В общий rich-text tokenizer добавлены legacy-закрытие Patchouli `/$`,
  переменные `$player=`, имена функций `setTextField(`, поля `.x` и resource ID
  вида `minecraft:overworld`. Они восстанавливаются из оригинала отдельно от
  русского текста.
- Ответ с кириллицей, но с длинным оставленным английским предложением теперь
  отклоняется и повторяется. Короткие названия продуктов и технические термины
  вроде `ME Advanced Pattern Provider` остаются допустимыми.
- AI-кэш самовосстанавливается при запуске и при каждом чтении записи. Пустые,
  совпадающие с исходником и явно неполные переводы удаляются; структурно
  неверная запись повторно переводится с актуальным FormatKit-контекстом и
  сразу атомарно сохраняется вместо старого значения.
- Перед первой санитарной очисткой создаётся резервная копия
  `ai_cache.json.pre-auto-repair`. Повреждённый JSON сохраняется как
  `ai_cache.json.corrupt`, а рабочий кэш немедленно создаётся заново.
- В фактическом кэше Beta30 обнаружены четыре неполных результата: AE2 Helpers,
  два абзаца AE2 Storage Types и дублированный английский текст PneumaticCraft.
  Новая проверка автоматически удаляет их для адресного повторного перевода.

### Проверка

- Полный набор: 327 тестов успешно.
- Реальный read-only аудит девяти сборок PrismLauncher: 2922 JAR, 2236
  книжных файлов, 24573 TranslationUnit, 0 ошибок no-op round-trip и 0 ошибок
  синтетической замены всего английского payload с обратной сборкой.
- Аудит locale-ресурсов нашёл 8556 JSON-файлов и 24693 переводимых JSON-узла.
  Новое структурное правило вернуло 1460 файлов с 5601 узлом, которые прежний
  список названий движков пропускал.
- Для локализованных Markdown-документов найдено 1226 реально существующих
  нетекстовых зависимостей, требующих переноса вслед за страницей; все пути
  вычислены по относительным ссылкам, без таблицы модов.
- Анализ и стартовый подсчёт книг совпали во всех девяти сборках. Для FTB
  Evolution: `11633 == 11633`; при одновременном выборе интерфейсов и книг:
  `134162 == 134162`.
- Отдельно `ftb-evolution-1.41.1`: 511 JAR, 491 книжный файл, 6678 единиц,
  0 структурных ошибок.
- В MAE2 проверены реальные многострочные Markdown-ссылки и `ItemLink`; четыре
  ранее неразбираемых файла теперь проходят lossless-проверку.
- Исходные JAR-файлы и каталоги сборок во время корпусных тестов не изменялись.

### Основные файлы

- `formatkit/contracts.py`
- `formatkit/registry.py`
- `formatkit/tokenizer.py`
- `formatkit/adapters/markdown.py`
- `formatkit/adapters/guideme.py`
- `formatkit/adapters/ie_manual.py`
- `mineai/processors/jar.py`
- `mineai/processors/analyzer.py`
- `mineai/processors/estimator.py`
- `tools/validate_formatkit_instances.py`
- `tests/test_formatkit_*.py`

---

## 10.0.0 — BETAv29

Дата сборки: 11.08.2026.

### Задача

Устранить оставшиеся расхождения между анализом, оценкой и фактической
обработкой Markdown, JSON-книг, SNBT и BetterQuesting. Обновление исходных
квестов не должно теряться из-за уже существующего целевого locale-файла.

### Что изменили

- Добавлен общий AST структурированных документов: типизированные пути,
  текстовые узлы, существующие переводы и lossless-сборка теперь используются
  адаптерами Markdown, JSON, SNBT и BetterQuesting.
- YAML front matter распознаётся только в начале Markdown. Горизонтальная линия
  `---` в теле книги больше не включает ложный YAML-режим и не скрывает
  последующий текст от перевода.
- `en_us.snbt` всегда остаётся структурной основой целевого locale. Старые
  переводы накладываются по идентификатору записи, имени поля и номеру
  повторения; новые поля автоматически добавляются и переводятся.
- Анализатор SNBT читает фактический целевой locale-файл и применяет ту же
  baseline-логику, что оценщик и процессор.
- Анализ JSON-книг использует ту же выборку, что перевод: технические значения
  больше не увеличивают число строк, а готовность сопоставляется по пути, а не
  по порядку значений.
- JSON-пути получили обратимое кодирование: `/` в ключах, строковые числовые
  ключи и индексы списков больше не смешиваются.
- Оценщики SNBT и BetterQuesting используют baseline для языков с общей
  латиницей и совпадают с фактическим процессором.
- Lossless-сборщик SNBT формирует результат одним линейным проходом вместо
  тысяч последовательных копирований строки. На реальном файле сборки время
  наложения locale сократилось примерно с 8,3 до 0,44 секунды.
- Обрывочные значения `'s` и `1x` признаются техническими до обращения к
  движку. Содержательные названия вроде `Steam Age` и `VoidShimmer Goo`
  остаются переводимыми; неизменённый ответ получает один строгий повтор с
  явным контекстом названия, затем сохраняется обычная цепочка fallback.
- Выбранные версия Minecraft, язык перевода и движок сохраняются в
  `settings.ini` и восстанавливаются при следующем запуске.

### Проверка

- Добавлены регрессии для всех исправленных сценариев и общего AST.
- Полный набор: 281 тест успешно.
- На тестовой `ftb-evolution-1.41.1` подтверждено чтение реального
  `ru_ru.snbt` и восстановление переводимых блоков после горизонтальных линий
  в книгах Applied Energistics 2 и дополнений.

### Основные файлы

- `mineai/formats/document.py`
- `mineai/formats/markdown.py`
- `mineai/processors/snbt_extract.py`
- `mineai/processors/translation_state.py`
- `mineai/processors/analyzer.py`
- `mineai/processors/estimator.py`
- `mineai/processors/selection.py`
- `mineai/engines/service.py`
- `mineai/gui_qt/main_window.py`
- `tests/test_beta29_regressions.py`

---

## 10.0.0 — BETAv28

Дата сборки: 11.08.2026.

### Задача

Lossless-модуль BETAv27 гарантировал сохранность разметки, но видимый текст всё
ещё мог переводиться по физическим строкам файла. Если автор книги перенёс одно
предложение внутри Markdown, модель получала обрывок наподобие `. The` и могла
корректно удалить английский артикль, после чего общий валидатор ошибочно
отклонял результат `.` из-за отсутствия русских букв.

### Что изменили

- Соседние обычные строки одного Markdown-абзаца теперь образуют одну логическую
  единицу перевода. Модель видит цельное предложение или абзац, а не произвольные
  физические обрывки.
- Произвольное объединение «по две строки» не используется: заголовки, списки,
  цитаты, YAML, таблицы, ссылки, теги, игровые коды и строки с жёстким переносом
  остаются самостоятельными узлами. Это сохраняет контекст без риска смешать
  разные элементы документа.
- После перевода русский абзац детерминированно распределяется по исходному
  количеству физических строк на границах слов. Skeleton BETAv27 отдельно
  восстанавливает исходные `CRLF`, `LF` или `CR`.
- Если безопасно разложить результат нельзя, весь абзац возвращается к исходному
  тексту; частично повреждённая страница не записывается.
- Общий валидатор разрешает для русского языка исчезновение отдельного
  английского артикля в пунктуационном фрагменте (`. The` → `.`). Правило
  применяется одинаково к LM Studio, KoboldCpp, прямому Google и Google fallback.
- Анализатор, оценщик, прогресс и обработчик Markdown теперь считают одинаковые
  логические единицы. В журнале они явно называются «смысловыми блоками».

### Проверка

- Добавлены регрессии для объединения физически перенесённого абзаца, сохранения
  границ Markdown-блоков, безопасной обратной раскладки и `. The` для локального
  ИИ и Google.
- Добавлена проверка совпадения анализа и оценки для переведённого составного
  абзаца.
- Полный набор: 266 тестов успешно.
- В 491 Markdown-файле тестовой сборки проверено 6161 смысловых блоков, включая
  407 объединённых абзацев и 9866 видимых текстовых узлов: 0 ошибок skeleton,
  выбора, tokenizer и переносов внутри узлов.

### Основные файлы

- `mineai/formats/markdown.py`
- `mineai/engines/service.py`
- `mineai/processors/analyzer.py`
- `mineai/processors/jar.py`
- `tests/test_markdown_safety.py`
- `tests/test_translation_service.py`
- `tests/test_analyzer_alignment.py`

---

## 10.0.0 — BETAv27

Дата сборки: 11.08.2026.

### Задача

BETAv26 всё ещё зависела от маркеров `[#N#]`: локальная модель могла удалить,
повторить или переставить маркер. Пересекающиеся регулярные выражения также
ошибочно считали экранированные звёздочки и `GUI/gui` изменёнными. В результате
безопасный русский текст отклонялся, хотя сама программа продолжала работу и
возвращала исходную английскую строку.

### Что изменили

- Добавлен отдельный модуль `mineai/formats/rich_text.py`. Он lossless-разбирает
  строку на неизменяемую разметку и видимый текст. В ИИ передаются только
  текстовые узлы; Markdown, ссылки, GuideME-теги, Patchouli-команды, цветовые
  коды, placeholders и технические термины модель больше не видит.
- Итоговая строка собирается из оригинальных неизменяемых фрагментов. Цвета и
  русский текст теперь действительно сохраняются независимо: даже полностью
  некорректный ответ ИИ не может изменить `$(#ED7014)`, `$()`, `<ItemLink ...>`,
  адрес `.md` или экранированный символ.
- Перевод, пытающийся ввести новую разметку внутрь обычного текстового узла,
  отклоняется локально; в результат возвращается безопасный исходный узел.
- Markdown-планирование и проверка структуры вынесены в
  `mineai/formats/markdown.py`. `MarkdownSkeleton` хранит исходные физические
  строки и каждый разделитель `CRLF`, `LF` или `CR`, а затем детерминированно
  собирает документ. Переносы больше не создаются через `"\n".join(...)`.
- Режим `Пропустить` также пересобирает существующий перевод через исходный
  skeleton, поэтому не подменяет переносы копией старого файла.
- Дублированная реализация Markdown удалена из `processors/selection.py`;
  анализатор, оценщик и обработчик используют один модуль формата.
- JSON-книги и Markdown-книги используют один безопасный форматированный путь.
  Поэтому исправление действует одинаково для LM Studio, KoboldCpp, прямого
  Google Translate и Google fallback.
- Исправлена маршрутизация: обычные `assets/*/lang/en_us.json` снова используют
  быстрый путь интерфейсной локализации, а книжные JSON — lossless-tokenizer и
  книжный промпт.
- Tokenizer дополнительно защищает ссылки со скобками в адресе, reference links,
  теги со знаком `>` внутри кавычек, `&#RRGGBB` и последовательности
  `§x§R§R§G§G§B§B`/`&x&R&R&G&G&B&B`.
- Все книжные запросы теперь явно используют книжный промпт и контекст, который
  включает мод и путь страницы.
- Узлы форматированного текста кэшируются с областью файла, строки и позиции.
  Одинаковые короткие фразы на разных страницах больше не получают перевод из
  неподходящего контекста.
- Защита терминов старого пути стала регистронезависимой: `GUI` и `gui`
  обрабатываются одинаково.
- Для пакетных запросов LM Studio используется официальный Structured Output:
  JSON Schema содержит точные ключи текущего пакета и запрещает лишние поля.
  Запросы ремонта обычного текста остаются без JSON-ограничения.

### Проверка

- Добавлены регрессии для реальной строки Little Big Redstone с
  экранированными `*`, цветов Apotheosis, GuideME/AE2-ссылок, тегов, `GUI/gui`
  и вложенных JSON text-компонентов.
- Полный набор: 260 тестов успешно.
- Корпус тестовой `ftb-evolution-1.41.1`: проверено 1146 Markdown-файлов и
  58 219 строковых JSON-значений; 0 ошибок lossless-сборки.
- План Markdown этой сборки: 9518 переводимых строк, 14 388 видимых текстовых
  узлов; 0 ошибок обратной сборки и 0 утечек маркеров `[#N#]` в текстовые узлы.
- Исправленная сборка выпущена как `MineAI_Translator_Beta27_fixed.exe`, потому
  что первоначальный `MineAI_Translator_Beta27.exe` во время сборки оставался
  запущен и был заблокирован Windows.

### Основные файлы

- `mineai/formats/rich_text.py`
- `mineai/formats/markdown.py`
- `mineai/engines/service.py`
- `mineai/engines/lmstudio.py`
- `mineai/processors/jar.py`
- `mineai/text_processing.py`
- `tests/test_rich_text_format.py`
- `tests/test_markdown_safety.py`
- `tests/test_translation_service.py`
- `tests/test_lmstudio.py`

---

## 10.0.0 — BETAv26

Дата сборки: 11.08.2026.

### Задача

Проверка результата BETAv25 выявила три независимые группы проблем:

- модель могла переставить уже известные коды Patchouli или придумать новый код
  наподобие `$(2mB)`, а валидатор сравнивал только количество маркеров;
- пакетный ответ иногда объединял соседние строки Markdown и присваивал один
  длинный перевод нескольким разным строкам;
- созданный ресурс-пак с руководством
  `immersiveengineering:manual` не был включён в Minecraft. В `options.txt`
  оставались активны только виртуальные пакеты Scholar, поэтому наличие
  перевода в ZIP не влияло на книгу.

### Что изменили

- Маркеры `[#N#]` теперь проверяются с точным порядком, а не только по
  количеству. Переставленный формат больше не считается корректным.
- После восстановления текста отдельно сравнивается упорядоченный набор
  Patchouli-команд, Minecraft-кодов, тегов, переменных и форматных параметров.
  Это отклоняет как перестановку цветов, так и придуманные конструкции.
- Внутри маскировщика используются служебные маркеры, которые не совпадают с
  Markdown-синтаксисом. Только после завершения разбора они преобразуются в
  публичные `[#N#]`. Поэтому сочетания `!**текст**`, `**текст**(...)`, тройное
  выделение и экранированные звёздочки больше не образуют ложные ссылки или
  картинки.
- Добавлена проверка крайних отклонений длины. Обрезанные ответы вида длинный
  абзац → `Экспорт` и ответы, в которые попал соседний абзац, не сохраняются.
- Если разные исходные строки одного пакета получили одинаковый длинный ответ,
  эти строки автоматически отправляются на повтор меньшими пакетами вплоть до
  перевода по одной.
- Те же проверки применены к Google Translate как к основному движку и как к
  fallback после локального ИИ. Google проверяет порядок маркеров до обратной
  подстановки, повторяет повреждённый ответ и разбирает подозрительно одинаковые
  пакетные ответы повторно по одной строке.
- Служебные количества вроде `1x <ItemLink ... />` считаются намеренно
  неизменяемыми и больше не увеличивают число ошибок или Google fallback.
- Старый AI-кэш BETAv25 автоматически выводится из использования один раз.
  Перед этим сохраняется резервная копия `ai_cache.json.pre-beta26`; новый кэш
  получает версию валидатора 26. Это исключает повторное использование уже
  размазанных переводов без безвозвратного удаления исходного файла.
- После успешного создания непустого ресурспака программа добавляет
  `file/<имя архива>.zip` в конец `resourcePacks` файла `options.txt`, то есть с
  наивысшим приоритетом. Если автоматическое включение невозможно, журнал прямо
  просит активировать архив вручную. Datapack-файлы не удаляются и не
  используются для клиентского руководства IE.

### Проверка Immersive Engineering

- В `Beta25v1_LM_Yandex.zip` найдено 113 русских файлов
  `assets/immersiveengineering/manual/ru_ru/*.txt`.
- В JAR мода найдено 114 английских файлов; единственный отсутствующий в
  ресурспаке `engineered_bricks.txt` пуст и не требует перевода.
- Для всех 113 созданных страниц совпадают количество строк и порядок
  служебных тегов/команд. Ошибка была вызвана неподключённым ресурспаком, а не
  путём `manual/ru_ru` и не старыми datapack-архивами.
- Предложенный скрипт из `решение книгиie.txt` использован как подтверждение
  принципа «переводить видимый текст, восстанавливать служебную часть отдельно».
  Сам скрипт не переносился: он обрабатывает лишь несколько типов строк и
  теряет остальные строки файла.

### Где изменили

- `mineai/text_processing.py` — безопасные внутренние маркеры, разбор Markdown,
  структурная и размерная проверка.
- `mineai/engines/llm_common.py` — порядок маркеров, поиск одинаковых пакетных
  ответов и повторный перевод.
- `mineai/engines/service.py` — финальная проверка форматирования, длины, кэша и
  защищённых служебных строк.
- `mineai/cache.py` — одноразовая миграция AI-кэша с резервной копией.
- `mineai/output/pack_writer.py`, `mineai/runtime/job.py` — включение созданного
  ресурспака и точный статус в журнале.
- `tests/test_regex_safety.py`, `tests/test_llm_common.py`,
  `tests/test_translation_service.py`, `tests/test_safe_writes.py` — сценарии
  из реального результата BETAv25 и `options.txt` с CRLF.

### Проверка

- Пройдено `244/244` автоматических теста и проверка `compileall`.
- Собран и пробно запущен `dist/MineAI_Translator_Beta26.exe`.
- SHA-256 EXE:
  `5AE44562B104BC639D199214B80705EC0CEBA83BB585992050A47FCA038F5C1B`.

---

## 10.0.0 — BETAv25

Дата сборки: 11.08.2026.

### Задача

В BETAv24 шестизначные цвета Patchouli внутри `$(#BB00BB)` и
`$(#ED7014)` ошибочно распознавались как составные технические названия. В
результате программа создавала вложенную конструкцию `$(#[#0#])`, не могла
вернуть цвет и отклоняла уже готовый русский текст целиком.

### Что изменили

- Коды Patchouli теперь маскируются целиком до поиска технических названий.
  Модель получает отдельный маркер вместо цвета, а программа после перевода
  возвращает исходный код без изменений.
- Защитили не только цветовые конструкции `$(#RRGGBB)`, но и остальные команды
  Patchouli, включая `$(p)` и сброс `$()`.
- Добавили ограниченное многопроходное восстановление старых вложенных маркеров.
  Если русский ответ уже получен и место кода известно, программа сохраняет
  перевод и отдельно возвращает точный цвет.
- Если модель полностью удалила маркер, программа не угадывает позицию цвета.
  Такой ответ по-прежнему отклоняется и отправляется на повторный перевод.

### Где изменили

- `mineai/text_processing.py` — порядок защиты фрагментов Patchouli и
  восстановление вложенных маркеров.
- `tests/test_regex_safety.py` — реальные сценарии с `BB00BB`, `ED7014`, `$()`
  и сохранением русского текста.

### Проверка

- Регрессионные тесты подтверждают, что в таблице маркеров больше не появляются
  отдельные `BB00BB` и `ED7014` или вложенные `$(#[#0#])`.
- Пройдено `231/231` автоматических тестов.
- Собран и запущен `dist/MineAI_Translator_Beta25.exe`.

---

## 10.0.0 — BETAv24

Дата сборки: 11.08.2026.

### Задача

Анализ BETAv23 не показывал `ae2:guide` и
`modern_industrialization:guidebook`. Обе книги хранят английские Markdown-файлы
без каталога `/en_us/`, тогда как старый анализатор считал `/en_us/`
обязательным.

### Что изменили

- Добавили структурный определитель Markdown-книг `MarkdownBookLocator`.
- Убрали зависимость от конкретных mod ID. Определитель ищет контейнеры
  `guide`, `manual`, `book`, `lexicon`, `handbook`, `codex` и `wiki` внутри
  `assets/<namespace>`.
- Сохранили три схемы локализации:
  - стандартная: `/en_us/` заменяется на `/ru_ru/`;
  - GuideME/AE2: `ae2guide/...` записывается в `ae2guide/ru_ru/...`;
  - Modern Industrialization: `mi_guidebook/...` записывается в
    `mi_guidebook/_ru_ru/...`.
- Научили программу определять стиль каталога по существующим локалям мода.
  Если JAR содержит `_ja_jp` или `_pt_br`, новая локаль получает тот же префикс.
- Исключили локализованные каталоги из списка английских источников.
- Запретили считать книгами `.txt` из `textures`, `models`, `sounds`,
  `shaders`, `blockstates` и `lang`.
- Передали один и тот же вычисленный путь анализатору, оценщику и обработчику
  JAR. Это исключило расхождение счётчика с фактической записью.
- Сохранили английский Markdown: ресурс-пак получает отдельный языковой путь и
  никогда не записывает перевод поверх исходной страницы.

### Актуализация по журналам LM Studio

Повторный запуск BETAv24 выявил три ошибки обработки Markdown. Версию приложения
оставили `10.0.0 - BETAv24`; исправленная сборка заменяет предыдущий EXE этой же
беты.

- Нормализовали `CRLF`, `CR` и `LF` перед разбором страницы. Символ `\r` больше
  не попадает в отдельную строку перевода и не вызывает ложный отказ
  `изменено количество переносов строк: 1 -> 0`.
- Валидатор структуры считает документы с `CRLF` и `LF` эквивалентными. Это
  устраняет ошибку `Unsafe Markdown translation` после корректного перевода
  страницы и при этом сохраняет проверку числа строк, ссылок, тегов и разметки.
- Служебные строки GuideME наподобие
  `* <ItemLink id="advanced_ae:walk_speed_card" />` больше не отправляются ИИ и
  Google. Если после тега расположен обычный текст, программа переводит только
  содержательную строку, сохраняя сам тег.
- Анализатор, оценщик и обработчик JAR теперь используют один план строк
  Markdown. Счётчик анализа больше не включает служебные теги, которые запись
  должна пропустить.
- Валидатор принимает корректное сокращение фразы из артикля и защищённого
  термина: например, `The UI` может стать `UI`. Остальной английский текст и
  повреждённые защищённые термины по-прежнему отклоняются.
- Журнал LM Studio содержит 52 завершённых ответа: все завершились с
  `finish_reason: stop`, без обрезки, переполнения контекста и ошибок памяти.
  Два первых отказа подключения обработал штатный повтор после запуска сервера.

### Дополнение по `ae2helpers` и `AE2wtlib`

- Защитили парные Markdown-маркеры `*…*`, `**…**` и обратные кавычки inline-
  кода до отправки модели. Перевод с другим количеством маркеров отклоняется,
  а ошибочная запись из кэша удаляется и переводится повторно.
- Смешанные буквенно-цифровые названия наподобие `AE2wtlib` теперь защищаются
  целиком. Валидатор больше не ищет отдельное `AE` внутри полного названия.
- Если итоговая проверка находит неизвестное повреждение Markdown, обработчик
  восстанавливает только проблемную строку из английского оригинала, удаляет её
  перевод из кэша и сохраняет остальные корректные строки страницы. Ошибка одной
  строки больше не отменяет перевод всей книги или JAR.

### Где изменили

- `mineai/processors/book_paths.py` — определение типа книги и целевого пути.
- `mineai/processors/analyzer.py` — обнаружение и подсчёт корневых книг.
- `mineai/processors/estimator.py` — оценка только тех страниц, которые реально
  обработает переводчик.
- `mineai/processors/jar.py` — безопасная запись в вычисленный языковой каталог.
- `mineai/processors/selection.py` — нормализация переносов и отбор строк с
  GuideME-тегами.
- `mineai/text_processing.py` — защита inline-разметки и составных технических
  названий.
- `mineai/engines/service.py` — проверка фраз из артикля и защищённого термина.
- `tests/test_analyzer_alignment.py` — совпадение анализа и оценки, защита от
  ложных книг.
- `tests/test_estimator_processor_parity.py` — реальные схемы AE2 и MI.
- `tests/test_markdown_safety.py`, `tests/test_release_blockers.py` — сценарии
  из журналов BETAv24.
- `docs/plans/2026-08-11-root-markdown-books-design.md` — проектное решение.

### Проверка

- На сборке `ftb-evolution-1.41.1` найдено 58 источников книг вместо 36.
- `Appliedenergistics2`: `0/2674` строк.
- `Modern-Industrialization-2.5.5`: `143/326`, 43%.
- `Immersiveengineering`: прежние `0/809`; стандартный формат не сломан.
- Пройдено `229/229` автоматических тестов.
- Собран и запущен `dist/MineAI_Translator_Beta24_fixed2.exe`; версия внутри
  приложения осталась `10.0.0 - BETAv24`.

---

## 10.0.0 — BETAv23

Дата сборки: 11.08.2026.

### Задача

Логи BETAv22 показали повреждение переменных Ars Nouveau, лишние переносы строк,
голые маркеры `#0#`, частично английские названия и неудачный ремонт маркеров.

### Что изменили

- Защитили шаблонные переменные `#tier#`, `#mana_cost#`, `#schools#` и другие
  значения вида `#name#`. Модель видит маркер, а программа возвращает точное
  исходное имя переменной после перевода.
- Расширили классификатор технических значений: отдельные `#header`, `#tier#` и
  `#mana_cost#` не отправляются переводчику.
- Добавили локальное исправление голого `#0#` в `[#0#]`. Для такой опечатки
  больше не нужен дополнительный запрос к модели.
- Запретили ремонту маркеров принимать английский оригинал вместо уже
  полученного русского текста.
- Для однострочного источника заменяем добавленный моделью перенос на пробел.
  Структурные переносы многострочного Markdown по-прежнему проверяются строго.
- Добавили проверку оставленного английского артикля. Результаты вида
  `The Футляр...` и `A Амулет...` отклоняются и повторно переводятся.
- Добавили русские подсказки терминологии для книг Building Gadgets:
  `Copy Paste Gadget`, `Cut Paste Gadget`, `Potion Charm` и `Gem Case`.

### Где изменили

- `mineai/text_processing.py` — маскирование `#variables#`, классификация
  технических строк и обработка переносов.
- `mineai/engines/llm_common.py` — ремонт маркеров, повторный перевод,
  терминологические подсказки и контроль частичного английского результата.
- `mineai/language_validation.py` — проверка английского артикля перед текстом
  на целевом языке.
- `mineai/engines/service.py` — финальная проверка кандидата и кэша.
- `tests/test_llm_common.py`, `tests/test_translation_service.py`,
  `tests/test_release_blockers.py` — регрессионные сценарии из реального лога.

### Проверка

- Пройдено `216/216` автоматических тестов.
- Собран и запущен `dist/MineAI_Translator_Beta23.exe`.
- Старый ресурс-пак с уже повреждёнными переменными требуется пересоздать;
  небезопасные записи старого кэша валидатор отбрасывает.

---

## 10.0.0 — BETAv22

Дата сборки: 11.08.2026.

### Задача

Первый тест LM Studio выявил ложные ошибки защищённых фрагментов, повреждение
угловых тегов, переносы внутри отдельных строк, лишнюю работу над техническими
значениями и отказ переводить обычные английские названия.

### Что изменили

- Исправили подсчёт пересекающихся защищённых терминов. Валидатор сначала ищет
  длинные значения: `RF/t` больше не создаёт второе ложное совпадение `RF`;
  аналогично работают `FE/t` и другие составные единицы.
- Защитили угловые теги перед нормализацией Minecraft-кодов. Тег `<&list>`
  больше не превращается в повреждённое `<&l ist>`.
- Добавили строгую проверку количества переносов строки для отдельного
  перевода и кэшированного результата. Запись с другой структурой удаляется из
  кэша и переводится повторно.
- Расширили определение технических строк: вызовы `getAreaTypes()`,
  `setCount()`, `setSide()`, `#header`, сочетания `NBT UI`, а также имена вроде
  `XNet` и `CuBee` не считаются ошибками перевода.
- Обычное название, совпавшее с оригиналом, получает отдельный повторный запрос
  с усиленным указанием перевести текст. Это исправило случай
  `Pocket Computer Addons`.
- Успешные результаты пакета сохраняются независимо от неудачных строк;
  повторный запрос содержит только отклонённые элементы.

### Где изменили

- `mineai/engines/service.py` — непересекающийся подсчёт фрагментов, проверка
  кандидатов и очистка небезопасного кэша.
- `mineai/text_processing.py` — защита угловых тегов и классификатор технических
  строк.
- `mineai/engines/llm_common.py` — усиленный повторный запрос для неизменённого
  переводимого текста и частичные повторы пакета.
- `tests/test_release_blockers.py`, `tests/test_translation_service.py`,
  `tests/test_llm_common.py` — воспроизведение ошибок из LM Studio.

### Проверка

- Проверили повторный запуск на логах MineAI и LM Studio.
- Собран `dist/MineAI_Translator_Beta22.exe`.

---

## 10.0.0 — BETAv21

Дата сборки: 11.08.2026.

### Задача

Добавить локальные модели через LM Studio, сохранив KoboldCPP и общий безопасный
конвейер перевода.

### Что изменили

- Добавили `LM Studio` как самостоятельный движок в основном списке.
- Создали отдельную вкладку настроек с URL, необязательным токеном, списком
  моделей, обновлением списка и проверкой соединения.
- Установили официальный OpenAI-compatible адрес по умолчанию:
  `http://localhost:1234/v1`.
- Подключили `/v1/models` и `/v1/chat/completions`.
- Нормализовали адрес: программа принимает как адрес сервера, так и путь с
  окончанием `/v1`.
- Сохраняем выбранную модель. Если пользователь ещё ничего не выбрал, интерфейс
  выбирает первую доступную текстовую модель.
- Передаём Bearer-токен только при заполненном поле.
- Переиспользуем один `requests.Session` между пакетами, сокращая накладные
  расходы HTTP и TCP.
- LM Studio не запускает KoboldCPP, не ищет GGUF и не управляет самой моделью.
  Пользователь запускает Local Server в LM Studio.
- Отмена задания прерывает повторные попытки без ложного сообщения о сетевой
  ошибке.
- Аналогичное переиспользование HTTP-сессии добавили KoboldCPP.
- Обновили русскую и английскую части README.

### Где изменили

- `mineai/engines/lmstudio.py` — API-клиент LM Studio.
- `mineai/engines/service.py` — создание выбранного провайдера.
- `mineai/config.py`, `mineai/constants.py` — URL, токен и модель.
- `mineai/gui_qt/dialogs.py`, `mineai/gui_qt/main_window.py`,
  `mineai/gui_qt/view_model.py` — вкладка настроек и состояние готовности.
- `mineai/gui_qt/i18n.py`, `mineai/gui_qt/i18n_runtime.py` — русские и
  английские сообщения.
- `mineai/runtime/job.py` — запуск задания через LM Studio.
- `tests/test_lmstudio.py` — соединение, модели, настройки, отмена и UI.
- `docs/plans/2026-08-11-lm-studio-provider-design.md` — проектное решение.

### Проверка

- Проверили реальный тестовый запуск по журналам MineAI и LM Studio.
- Собран `dist/MineAI_Translator_Beta21.exe`.

---

## 10.0.0 — BETAv20

Дата сборки: 10.08.2026.

### Задача

Исправить пустые `resourcepack`/`datapack`, показывать только реальные результаты
и расширить анализ FTB Quests на таблицы наград.

### Что изменили

- `PackWriter.close()` удаляет новый ZIP, если в нём остался только
  `pack.mcmeta`, и возвращает `None` вместо несуществующего результата.
- Если полезные данные записаны только в один архив, программа сохраняет и
  показывает только этот архив.
- Итоговый журнал выводит фактически созданные архивы и фактически изменённые
  файлы. Предварительно рассчитанные пути больше не выдаются за результат.
- Процессоры прямой записи возвращают путь только после успешного изменения.
- Добавили поля таблиц наград FTB `item_name`, `flavor_text` и `name`.
- Оставили технические поля `id`, `string_id`, идентификаторы предметов и числа
  без перевода.
- Каждый `config/ftbquests/quests/reward_tables/*.snbt` отображается отдельным
  выбираемым элементом.
- FTB Quests и BetterQuesting по принятому решению продолжают сохраняться в
  соответствующие файлы `config`. BETAv20 не создаёт отдельный загрузчик для
  переноса этих форматов в datapack.
- Статус остановленного анализа или перевода сохраняет достигнутый прогресс и
  не подменяется ложным сообщением об успешном завершении.

### Где изменили

- `mineai/output/pack_writer.py` — учёт полезных записей и удаление пустых ZIP.
- `mineai/runtime/job.py` — сбор фактических результатов и итоговый отчёт.
- `mineai/processors/analyzer.py` — обнаружение таблиц наград.
- `mineai/processors/snbt_extract.py`, `mineai/processors/snbt.py` — безопасный
  выбор и запись отображаемых полей.
- `tests/test_safe_writes.py`, `tests/test_job_terminal_status.py`,
  `tests/test_quest_group_selection.py` — пустые архивы, реальные пути и reward
  tables.
- `docs/plans/2026-08-10-output-report-and-reward-tables-design.md` — проектное
  решение.

### Проверка

- Полный набор тестов версии прошёл.
- Собран и запущен `dist/MineAI_Translator_Beta20.exe`.

---

## 10.0.0 — BETAv19

Дата сборки: 10.08.2026.

### Задача

Убрать длинный список выбора из главного окна, сделать крупные FTB-квесты
выбираемыми по главам и устранить конфликт цветов строки, наведения и чекбокса.

### Что изменили

- Перенесли полный список в отдельное модальное окно
  «Выбор содержимого для перевода».
- В главном окне оставили компактный счётчик и кнопку «Настроить выбор…».
- Добавили дерево с модами, книгами, квестовыми файлами и дочерними группами.
- Добавили поиск, фильтр по типу, «Выбрать всё», «Снять всё» и счётчик выбранных
  конечных элементов.
- Кнопка «Отмена» сохраняет предыдущий выбор; «Применить» передаёт новое
  состояние заданию.
- Монолитный `config/ftbquests/quests/lang/en_us.snbt` сопоставляется с файлами
  `quests/chapters` по шестнадцатеричным ID.
- Группы получают названия глав. Несопоставленные записи попадают в
  «Прочие записи».
- Группа крупнее 500 строк делится на устойчивые части. Если файлов глав нет,
  делится весь язык квестов.
- Индекс `ID → строки` строится за один проход, поэтому разделение не сканирует
  файл заново для каждой записи.
- Оценщик и SNBT-процессор получают одинаковые сегментные ключи. Снятая глава
  не меняется даже при совпадающем тексте с выбранной главой.
- Отключили обычное выделение строк в дереве. Чекбокс, наведение и фокус получили
  разные цвета в тёмной и светлой темах.

### Где изменили

- `mineai/gui_qt/dialogs.py` — отдельное окно и дерево выбора.
- `mineai/gui_qt/main_window.py` — кнопка и компактная сводка.
- `mineai/gui_qt/theme.py` — контрастные состояния строк и чекбоксов.
- `mineai/processors/quest_groups.py` — группы глав и технические части.
- `mineai/processors/snbt_extract.py`, `mineai/processors/snbt.py` — индекс ID и
  фильтрация выбранных записей.
- `mineai/processors/estimator.py` — оценка выбранных частей.
- `tests/test_quest_group_selection.py`, `tests/test_qt_windows_ux.py` — главы,
  выбор и оформление.
- `docs/plans/2026-08-10-translation-selection-dialog-design.md` — проектное
  решение.

### Проверка

- Проверили выбор отдельной главы, обновление существующего `ru_ru.snbt` и
  совпадение оценки с обработчиком.
- Собран `dist/MineAI_Translator_Beta19.exe`.

---

## 10.0.0 — BETAv18

Дата версии: 10.08.2026.

### Задача

После анализа дать пользователю возможность отключить отдельный мод, книгу или
квест до запуска перевода.

### Что изменили

- Анализатор начал отдавать структурированные элементы вместо одной текстовой
  таблицы в журнале.
- Каждый элемент включён по умолчанию и имеет стабильный ключ из абсолютного
  пути, области и, при необходимости, сегмента.
- Один JAR создаёт независимые области `mods` и `books`: можно перевести книгу,
  не переводя интерфейс того же мода, и наоборот.
- Снятая галочка исключает элемент одновременно из оценки и обработки.
- Невыбранные исходные и целевые файлы не открываются для записи.
- Пустой выбор после анализа блокирует запуск. Значение выбора `None` сохраняет
  совместимость со старым поведением «обработать все выбранные категории».
- Выбор сбрасывается после нового анализа или смены папки Minecraft, чтобы пути
  одной сборки не применились к другой.
- Первая версия списка находилась непосредственно в главном окне; в BETAv19 он
  был перенесён в отдельный диалог.

### Где изменили

- `mineai/analysis_items.py` — модель элемента и стабильные ключи.
- `mineai/processors/analyzer.py` — создание элементов анализа.
- `mineai/runtime/job.py` — фильтрация оценки и обработки.
- `mineai/gui_qt/main_window.py`, `mineai/gui_qt/view_model.py` — список и
  состояние выбора.
- `tests/test_analysis_item_selection.py` — независимый выбор областей JAR и
  полный пропуск снятого квеста.
- `docs/plans/2026-08-10-analysis-item-selection-design.md` — проектное решение.

### Проверка

- Проверили независимый выбор интерфейса и книги одного JAR.
- Проверили, что снятый квест не передаётся процессору.
- Отдельная копия BETAv18 сейчас отсутствует в `dist`; версия подтверждается
  рабочим интерфейсом и проектными материалами. Следующая сохранённая сборка —
  BETAv19.

---

## 10.0.0 — BETAv17

Дата сборки: 10.08.2026.

### Задача

Исключить повреждение Markdown/GuideME при переводе книг, прежде всего на
материале руководства Applied Energistics 2.

### Что изменили

- Разделили переводимый текст и структуру Markdown.
- Защитили YAML front matter, заголовки, пустые строки, теги GuideME,
  изображения и адреса Markdown-ссылок.
- Оставили переводимым видимый текст ссылки, но маскируем `[` и `](url)`
  отдельными маркерами.
- Добавили сигнатуру структуры документа: количество строк и тип каждой строки
  должны совпасть до и после перевода.
- Существующий перевод с другой структурой страницы игнорируется.
- Перед записью обработчик повторно проверяет итоговый документ. При повреждении
  Markdown файл не попадает в ресурс-пак и программа сообщает об ошибке.
- Режим «умной склейки» больше не меняет структуру Markdown-страницы.
- Добавили безопасную работу с JSON text-компонентами и защищёнными
  Minecraft-фрагментами в тексте страниц.

### Где изменили

- `mineai/processors/selection.py` — выбор строк и сигнатура Markdown.
- `mineai/processors/jar.py` — чтение существующего перевода, сборка страницы и
  финальная проверка перед записью.
- `mineai/text_processing.py` — маскирование ссылок, тегов и форматирования.
- `mineai/engines/service.py` — проверка защищённых фрагментов результата.
- `tests/test_markdown_safety.py`, `tests/test_regex_safety.py` — повреждённые
  ссылки, лишние строки, YAML и полный цикл mask/unmask.

### Проверка

- Проверили воспроизведение проблемы на руководстве AE2.
- Проверили отказ от записи документа с изменённой структурой.
- Собран `dist/MineAI_Translator_Beta17.exe`.

---

## Артефакты сборок

В каталоге `dist` сейчас сохранены:

- `MineAI_Translator_Beta17.exe`;
- `MineAI_Translator_Beta19.exe`;
- `MineAI_Translator_Beta20.exe`;
- `MineAI_Translator_Beta21.exe`;
- `MineAI_Translator_Beta22.exe`;
- `MineAI_Translator_Beta23.exe`;
- `MineAI_Translator_Beta24.exe`.

BETAv18 существовала как рабочая промежуточная версия, но отдельный EXE под этим
именем в текущем каталоге не сохранён.

## Как обновлять этот журнал

Для каждой следующей версии добавляйте раздел над предыдущей версией и
указывайте:

1. задачу и исходную ошибку;
2. изменённое пользовательское поведение;
3. формат данных или алгоритм, если он важен для безопасности;
4. основные файлы реализации;
5. тесты, реальную проверку и имя собранного EXE;
6. известные ограничения или необходимость пересоздать старый результат.
